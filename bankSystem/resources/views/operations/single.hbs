{{>header.layout}}
{{>navbar.layout}}
<section class="container my-5">
    {{#if operation}}
    <div class="alert alert-success">
        <ul>
        <li>{{operation.amount}}</li>
        <li>{{operation.date}}</li>
        <li>{{operation.location}}</li>

        </ul>
    </div>
    
    {{else}}
        <div class="alert alert-danger">
            no data found
        </div>
    {{/if}}
</section>
{{>footer.layout}}